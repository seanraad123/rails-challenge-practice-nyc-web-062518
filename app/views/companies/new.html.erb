<h1>Create New Company</h1>

<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= collection_select :office, :building_id, Building.all, :id, :name %>
  <%# collection_select :office, :building_id, Building.all, :id, :name %>
  <%= f.submit %>

<% end %>
