<h1>Create New Company</h1>

<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.collection_select :company, :building_id, Building.all, :id, :name %>
  <!-- <%= f.collection_select :buildings, Building.all, :id, :floor %> -->
  <%= f.submit %>
<% end %>
